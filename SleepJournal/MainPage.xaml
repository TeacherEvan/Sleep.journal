<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SleepJournal.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">

            <Label
                Text="Journal Entry"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Editor
                Text="{Binding Text}"
                MaxLength="200"
                Placeholder="Enter your thoughts..."
                HeightRequest="100" />

            <Label
                Text="{Binding ErrorMessage}"
                TextColor="Red" />

            <Label Text="Mood (1-10)" />
            <Slider
                Minimum="1"
                Maximum="10"
                Value="{Binding Mood}" />
            <Label Text="{Binding Mood}" />

            <Label Text="Social Anxiety (1-10)" />
            <Slider
                Minimum="1"
                Maximum="10"
                Value="{Binding SocialAnxiety}" />
            <Label Text="{Binding SocialAnxiety}" />

            <Label Text="Regretability (1-10)" />
            <Slider
                Minimum="1"
                Maximum="10"
                Value="{Binding Regretability}" />
            <Label Text="{Binding Regretability}" />

            <Button
                Text="Save"
                Command="{Binding SaveCommand}"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
