# TODO — Audio Playback Wiring (Sleep Journal)

Plan status: PROPOSED. Objectives below are the execution prompt for this run.

## Objectives

- [ ] **OBJ-001** — Define `IAudioPlayer` interface (`PlayAsync(byte[] wav, float volume, CancellationToken)`) in `SleepJournal/Services/IAudioPlayer.cs`. *Req: FR-001, FR-003. AC: interface compiles; signature is testable.*
- [ ] **OBJ-002** — Implement `PlatformAudioPlayer` default impl in `SleepJournal/Services/PlatformAudioPlayer.cs`; per-platform `#if ANDROID/iOS/WINDOWS/MACCATALIST` bodies route bytes to a real player; non-MAUI/non-platform body logs "no audio player available" and returns. *Req: FR-001, FR-003, NFR-002. AC: net9.0 build green; Android-target syntax valid.*
- [ ] **OBJ-003** — Wire `AudioService` to the player: ctor gains optional `IAudioPlayer` param (default `PlatformAudioPlayer`); `PlayWavAsync` delegates to `_player.PlayAsync` instead of no-op'ing. *Req: FR-001. AC: PlayWavAsync calls player (proven by mock test).*
- [ ] **OBJ-004** — Preserve exception safety: `PlayDropSoundAsync`/`PlayClickSoundAsync` keep their try/catch so a player failure is logged and swallowed, never thrown to the ViewModel. *Req: FR-002, FR-003. AC: player-throws test passes.*
- [ ] **OBJ-005** — Mute honoured at playback: when `_audioEnabled` is false, the player must receive zero calls (not just skip synthesis). *Req: FR-005. AC: muted-no-call test passes.*
- [ ] **OBJ-006** — Register `PlatformAudioPlayer` in `MauiProgram.cs` DI (singleton). *Req: NFR-001. AC: MauiProgram compiles; no new packages.*
- [ ] **OBJ-007** — Extend `AudioServiceTests.cs` with real (non-tautological) facts: player mock injected and called with generated bytes; muted -> zero calls; player throws -> no propagation; volume forwarded. *Req: AC-003..006. AC: new tests pass; break-it check (stub the player throw path) still fails the propagation test.*
- [ ] **OBJ-008** — No regression: full `dotnet build -f net9.0` (0 errors) and `dotnet test` (>=125 passed, 0 failed). *Req: NFR-002, NFR-003. AC: gate output captured.*
- [ ] **OBJ-009** — Update `IMPLEMENTATION_STATUS.md` Phase 4 note: mark audio playback routing COMPLETE with the commit SHA, leaving the cloud-sync / analytics checkboxes open. *Req: documentation accuracy. AC: status doc matches live code.*
- [ ] **OBJ-010** — Commit scoped source (no audit scratch, no unrelated files) and push only if `PUSH=1`. *Req: NFR-004. AC: `git diff --name-only` shows only the intended paths + the plan/status docs.*

## Definition of Done
All objectives ticked; build green; tests >=125 with 0 failures; status doc updated;
commit contains only the scoped change set; audit scratch left untracked.
